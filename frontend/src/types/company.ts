export interface Company {
    id: string
    name: string
    language: string
    currency: string
    VAT: string
    address: string
    postalCode: string
    city: string
    country: string
    phone: string
    email: string
}
